function tenTimes(number){
    const result = number * 10;
    return result;
}

tenTimes(5);
const total = tenTimes(10);
console.log(total);

function cutHalf(number) {
    const half = number / 2;
    return half;
}

