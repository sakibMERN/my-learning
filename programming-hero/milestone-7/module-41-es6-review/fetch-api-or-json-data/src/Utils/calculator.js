const add = (first, second) => {
    return first + second;
}

const calculator = (num1, num2) => {
    return num1 * num2;
}

const divideTheFirstNumberByTheSecondNumber = (first,second) => {
    return first / second;
}

export {add, calculator, divideTheFirstNumberByTheSecondNumber}